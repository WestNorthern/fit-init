<%= form_with model: @update_score, id: "hourly_score", class: "custom" do |f| %>

  <%= f.hidden_field :hydrated, :value => true %>

  
  <input type="submit" name="commit" value="H2O!" class="hollow button secondary" id="H2O" data-open="exampleModal3" data-disable-with="H2O!">

<% end %>